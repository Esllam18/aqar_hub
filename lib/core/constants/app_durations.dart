class AppDurations {
  static const Duration instant = Duration(milliseconds: 0);
  static const Duration veryShort = Duration(milliseconds: 500);
  static const Duration short = Duration(milliseconds: 800);
  static const Duration medium = Duration(milliseconds: 1200);
  static const Duration long = Duration(milliseconds: 1600);
  static const Duration veryLong = Duration(milliseconds: 2200);
  static const Duration extraLong = Duration(milliseconds: 3000);
}
